const fs = require('fs');
const path = require('path');

const {
  ERRORS,
  getErrorMessage
} = require('../utils/errorMessageHandler');

class FileOperator {
  
}

module.exports = {
  FileOperator
};
